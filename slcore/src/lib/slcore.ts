import { SlButton, SlDivider, SlDropdown, SlIcon, SlMenu, SlMenuItem } from "@shoelace-style/shoelace";

export default [SlButton, SlDivider, SlDropdown, SlIcon, SlMenu, SlMenuItem];
// export default SlDivider;
